spring:
  datasource:
    url: jdbc:mysql://localhost:3306/contracttest
    username: root
    password: DesertSt0rm
    driver: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
  hibernate:
    ddl-auto: update
    naming-strategy: org.hibernate.cfg.ImproveNamingStrategy
  properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect















